name: ao_gosto_estoque
description: Controle de estoque de bebidas - Fardos e Avulsas
version: 1.0.2+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_preferences: ^2.5.4
  intl: ^0.19.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/go-icon.png
    - shorebird.yaml  # <--- ADICIONE ESSA LINHA OBRIGATÓRIA